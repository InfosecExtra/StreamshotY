Still having threading errors…..working on it. Have threading set to 1 for now.

Install Littleshot link here: https://github.com/BoredHackerBlog/littleshot

Compose Littleshot: docker-compose up -d --build caddy

Open browser to the location specified littleshot_server

Optional: *Specify a word to match on for cert stream. Default is “office”

Run the app.py

See results in terminal, browser, and/or “Yaramatches.txt”

FIN.
